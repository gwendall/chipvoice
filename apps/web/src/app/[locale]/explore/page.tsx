import Explore from "@/community/Explore";
import { pageMetadata } from "@/i18n/metadata";
export const generateMetadata = pageMetadata("explore");
export default function Page() {
  return <Explore />;
}
