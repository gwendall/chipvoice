# Ricoh 2A03 (`2a03`)

The NES and Famicom's sound, as the CPU had it on the same die: two pulses, a
triangle, a noise generator, and a sample channel, the DMC. The method behind every section
is in [CONFORMANCE.md](../CONFORMANCE.md).

| | |
| --- | --- |
| **Machine** | NES, Famicom (NTSC clock, 1789773 Hz) |
| **Status** | **in progress**: every test ROM passes, the pulses match the reference, the analog stage is unmeasured |
| **Core** | own, `packages/chipvoice/src/chips/nes/dsp.ts` |
| **Licence of the core** | MIT |
| **Sheet updated** | 2026-09-04, by hand |

## Digital parity

Measured by [`conform`](../../packages/conform), the harness, against
[Nes_Snd_Emu 0.1.7](../../packages/conform/oracles/nes-snd-emu), on the two
pulses, the triangle and the DMC, over this project's own songs and ten scripts in
[`packages/conform/corpus/2a03`](../../packages/conform/corpus/2a03). The numbers
between the markers are written by the harness (`pnpm --filter chipvoice-conform
baseline`); the reading of them below is a person's. CI reruns the corpus on every
push and fails if any voice's identical count falls below the committed baseline.

<!-- parity:begin -->
Written by `conform` on 2026-09-04, against Nes_Snd_Emu 0.1.7 (blargg), on p1, p2, tri, dmc.

| | |
| --- | --- |
| Oracle | Nes_Snd_Emu 0.1.7 (blargg) |
| Corpus | 13 logs, 92173307 cycles |
| Identical cycles | 2228909 / 92173307 (2.4182 %) |
| Logs with a divergence | 13 |

| Log | Identical | First divergence | Per voice: identical; edges exact / near / unmatched; best constant shift; runs aligned under a shift of their own |
| --- | --- | --- | --- |
| script-dmc | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 0/0/0; p2 100.0000 %, 0/0/0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 3.7245 %, 0/0/52207; runs 3: 2 on times, 0 on values, shift <= 54 |
| script-envelope | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 99.9987 %, 638/0/48; runs 3: 0 on times, 0 on values, shift <= 0; p2 99.9805 %, 4876/1/721; runs 45: 0 on times, 0 on values, shift <= 0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| script-five-step | 0.3589 % | cycle 0, tri: ours 15, oracle 0 | p1 99.9998 %, 220/9/0; runs 2: 1 on times, 1 on values, shift <= 0; p2 100.0000 %, 615/1/0; runs 2: 2 on times, 2 on values, shift <= 0; tri 0.3590 %, 0/0/882; runs 3: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| script-length | 1.4191 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 1029/0/0; runs 3: 3 on times, 3 on values, shift <= 0; p2 100.0000 %, 0/0/0; tri 1.4191 %, 0/0/2794 (17 at -4); runs 4: 1 on times, 0 on values, shift <= 1788787; dmc 100.0000 %, 0/0/0 |
| script-linear | 0.8936 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 0/0/0; p2 100.0000 %, 0/0/0; tri 0.8936 %, 0/0/1738 (5 at -4); runs 4: 1 on times, 0 on values, shift <= 1789684; dmc 100.0000 %, 0/0/0 |
| script-low-pulse | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 219/0/0; runs 219: 219 on times, 219 on values, shift <= 0; p2 100.0000 %, 0/0/0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| script-noise | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 0/0/0; p2 100.0000 %, 0/0/0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| script-restart | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 471/0/0; runs 1: 1 on times, 1 on values, shift <= 0; p2 100.0000 %, 0/0/0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| script-sweep-down | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 308/0/0; runs 3: 3 on times, 3 on values, shift <= 0; p2 100.0000 %, 292/0/0; runs 3: 3 on times, 3 on values, shift <= 0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| script-sweep-up | 0.0000 % | cycle 0, tri: ours 15, oracle 0 | p1 99.9066 %, 373/0/702; runs 1: 0 on times, 0 on values, shift <= 0; p2 99.8808 %, 2002/0/4480 (2239 at -2); runs 1: 0 on times, 0 on values, shift <= 0; tri 0.0000 %, 0/0/1; runs 1: 0 on times, 0 on values, shift <= 0; dmc 100.0000 %, 0/0/0 |
| song-e2e | 5.1820 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 8602/0/0; runs 32: 32 on times, 32 on values, shift <= 0; p2 100.0000 %, 4966/0/0; runs 2428: 2428 on times, 2428 on values, shift <= 0; tri 5.1820 %, 0/0/27887; runs 52: 37 on times, 0 on values, shift <= 358350; dmc 100.0000 %, 0/0/0 |
| song-golden | 2.6083 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 3197/0/0; runs 13: 13 on times, 13 on values, shift <= 0; p2 100.0000 %, 2077/0/0; runs 1038: 1038 on times, 1038 on values, shift <= 0; tri 2.6083 %, 0/0/11866 (17 at -14); runs 21: 14 on times, 0 on values, shift <= 357856; dmc 100.0000 %, 0/0/0 |
| song-studio | 4.5000 % | cycle 0, tri: ours 15, oracle 0 | p1 100.0000 %, 9871/0/0; runs 32: 32 on times, 32 on values, shift <= 0; p2 100.0000 %, 5803/0/0; runs 2816: 2816 on times, 2816 on values, shift <= 0; tri 4.5000 %, 0/0/32641; runs 62: 46 on times, 0 on values, shift <= 358635; dmc 100.0000 %, 0/0/0 |
<!-- parity:end -->

**What the numbers say.** The two pulses are identical to the oracle cycle for
cycle on all three songs: every edge exact, none unmatched, every run aligned at
a shift of zero. So are the sweep-unit mute and its `$4001 = $08` cure, the
`$4003` phase restart, and the downward sweep on both pulses, including pulse 1's
extra minus one. What differs falls into three classes, and none of them is a
chipvoice bug against nesdev; each is a convention of a 2005 oracle that the
documentation has since moved past.

1. **Frame timing.** The oracle's frame steps land two cycles after nesdev's
   7457, 14913, 22371, 29829, which `test/clock.mjs` pins. Anything the frame
   counter drives - an envelope step, a sweep step, a length or linear counter
   running out - happens two cycles later in the oracle, and a pulse whose
   period the sweep changed keeps a phase offset from then on. That is
   `script-envelope`, `script-sweep-up`, the pulse lines of `script-five-step`
   and the tail of `script-length`. Settled: blargg's own test ROMs, run below,
   check the frame timing to the cycle and pass against this core, so the
   hardware's timing is nesdev's and the 2005 oracle is the one that is off.
2. **The triangle's sequencer position.** The oracle powers its triangle on
   at 0; the hardware powers on at 15, and so does this core, since blargg's
   mixer test proved it (below). So the two differ on every silent cycle from
   the first, which is why every triangle line reads close to nothing
   identical. And when its counters reload, the oracle steps at once where the
   hardware waits for the timer's next expiry, so from the first note on the
   two sequencers are a couple of steps apart for good. The runs column is the
   number to read: the harness puts the sequence's hidden steps back and finds
   the oracle stepping on the same cycles, under a shift of at most a period.
3. **Envelope at power-on.** The oracle's `reset()` writes `$4003` to every
   voice, which leaves its envelope start flag set; its first frame clock then
   loads every decay level with 15. A voice put in envelope mode before that has
   decayed sounds at 15 in the oracle and at 0 here, where nesdev says the decay
   level is 0 until a `$4003` write is processed. The scripts start a tenth of a
   second in so that this does not colour them.

4. **The DMC's first cycle.** Its output unit starts silent with eight bits
   to go in ours, as nesdev describes it and Mesen does it, and with one in the
   oracle, so the first sample byte plays one bit period apart: every step of
   `script-dmc` lands 54 cycles later in the oracle, with the same values. And
   a `$4011` write in the oracle adjusts the amplitude through a DAC table, for
   the sound of the pop rather than the register's value, so the levels after
   one are the oracle's own. The steps still align.

The noise is not compared: the oracle's register starts elsewhere, runs with the
inverse polarity and is not clocked exactly while muted, so its bit pattern
cannot match anything. Its rates and sequence lengths are pinned by the formula
tests, and its envelope shares the pulses' code.

## Test ROMs

Run by the harness on a 6502 it carries for the purpose
(`pnpm --filter chipvoice-conform roms`), in CI on every push. Blargg's four
suites: `apu_test` and `apu_reset` from 2011, which report through memory at
`$6000`; `dmc_tests`, which report by beeping; and the 2005 frame counter set,
which prints a code on screen. Every one of them passes. Between them they check
the length counters and their table, both frame sequences to the cycle, the
frame IRQ flag and the interrupt itself, the APU's clock jitter, what power-on
and the reset button do, the DMC's rates, status, buffer, latency and IRQ, and
two things only the hardware knew: a halt flag written on a length clock's cycle
takes effect after it, and a length reload on that cycle is ignored unless the
counter was zero.

What they do not check, by their author's word: the envelope, the sweep and the
triangle's linear counter near a clock, and any channel's output. `apu_mixer`,
which listens to the DAC, is not run; it wants an ear.

<!-- roms:begin -->
Run by `conform`'s 6502 fixture on 2026-09-04: 29 of 29 pass.

| ROM | Result | What it said |
| --- | --- | --- |
| `apu_test/1-len_ctr` | pass | 1-len_ctr Passed |
| `apu_test/2-len_table` | pass | 2-len_table Passed |
| `apu_test/3-irq_flag` | pass | 3-irq_flag Passed |
| `apu_test/4-jitter` | pass | 4-jitter Passed |
| `apu_test/5-len_timing` | pass | 5-len_timing Passed |
| `apu_test/6-irq_flag_timing` | pass | 6-irq_flag_timing Passed |
| `apu_test/7-dmc_basics` | pass | 7-dmc_basics Passed |
| `apu_test/8-dmc_rates` | pass | 8-dmc_rates Passed |
| `apu_reset/4015_cleared` | pass | 4015_cleared Passed |
| `apu_reset/4017_timing` | pass | Delay after effective $4017 write: 9 4017_timing Passed |
| `apu_reset/4017_written` | pass | 4017_written Passed |
| `apu_reset/irq_flag_cleared` | pass | irq_flag_cleared Passed |
| `apu_reset/len_ctrs_enabled` | pass | len_ctrs_enabled Passed |
| `apu_reset/works_immediately` | pass | works_immediately Passed |
| `dmc_tests/buffer_retained` | pass | 1 beep |
| `dmc_tests/latency` | pass | 1 beep |
| `dmc_tests/status` | pass | 1 beep |
| `dmc_tests/status_irq` | pass | 1 beep |
| `apu_2005/01.len_ctr` | pass |  $01 |
| `apu_2005/02.len_table` | pass |  $01 |
| `apu_2005/03.irq_flag` | pass |  $01 |
| `apu_2005/04.clock_jitter` | pass |  $01 |
| `apu_2005/05.len_timing_mode0` | pass |  $01 |
| `apu_2005/06.len_timing_mode1` | pass |  $01 |
| `apu_2005/07.irq_flag_timing` | pass |  $01 |
| `apu_2005/08.irq_timing` | pass |  $01 |
| `apu_2005/09.reset_timing` | pass |  $01 |
| `apu_2005/10.len_halt_timing` | pass |  $01 |
| `apu_2005/11.len_reload_timing` | pass |  $01 |
<!-- roms:end -->

## Formula tests

`packages/chipvoice/test/clock.mjs`, in CI on every push.

| Test | Result |
| --- | --- |
| Pulse rate is CPU / (16 (t + 1)) | pass |
| Triangle rate is CPU / (32 (t + 1)) | pass |
| Noise at rate 15 shifts 440 times a second | pass |
| Noise at rate 0 shifts every four CPU cycles | pass |
| Long noise sequence is 32767 steps | pass |
| Short noise sequence is 93 steps from power-on | pass |
| Quarter frames at 240 Hz, half frames at 120 Hz | pass |
| Sequence steps at 7457, 14913, 22371, 29829 cycles | pass |
| Half frames on the second and fourth steps | pass |
| DMC raises its level by 2 a bit, 54 cycles a bit at rate 15, and stops at 126 | pass |
| `$4011` sets the level at once; zeros lower it at 428 cycles a bit from the ninth bit period | pass |
| Clearing the DMC's `$4015` bit plays out the two bytes in hand, then holds | pass |

## Analog stage

The mixer is measured; the filters are not.

| | |
| --- | --- |
| Profile | `nesdev`: first-order high-pass at 90 Hz and 440 Hz, first-order low-pass at 14 kHz, the values nesdev gives as approximate, and a make-up gain of 2.9. The only profile; `NesOutputStage` takes another |
| Reference unit, mixer | blargg's NES, through his `apu_mixer` recordings |
| Reference unit, filters | none chosen |
| Capture | none of our own |
| Tolerance | the three cancellation tests at or below -25 dB; the noise test within 4 dB of the recording |
| Maximum band error | not measured |
| Corners measured | not measured |
| Resampling | a box filter over the CPU cycles that make up each output sample; not band-limited |

**The mixer, against the hardware.** Blargg's four `apu_mixer` ROMs play a
tone, then have one channel play a waveform while the DMC's DAC plays its
inverse, then play a tone again: if the mixer's non-linearity is the hardware's,
the middle is nearly silent, and he recorded the four on a real NES. The harness
runs each ROM on its 6502, renders the writes through the whole core, and
measures the middle's level against the tone's, on our render and on his
recording (`pnpm --filter chipvoice-conform mixer`, in CI). This is the DAC
curves measured, which is where most of the identity of the sound is.

<!-- mixer:begin -->
Written by `conform` on 2026-09-04. The middle's level relative to the tone's; lower is a better cancellation.

| Test | This core | Blargg's NES, his recording |
| --- | --- | --- |
| `apu_mixer/square` | -32.7 dB | -32.2 dB |
| `apu_mixer/triangle` | -33.0 dB | -30.9 dB |
| `apu_mixer/noise` | -13.8 dB | -16.2 dB |
| `apu_mixer/dmc` | -31.0 dB | -27.2 dB |
<!-- mixer:end -->

The mixer is the nesdev non-linear approximation, `95.88 / (8128 / (p1 + p2) + 100)`
for the pulses and `159.79 / (1 / (t/8227 + n/12241 + d/22638) + 100)` for the
rest, and it cancels as well as blargg's own console does. A fixed gain of 2.9
follows the filters for headroom; it is not hardware and will be replaced by a
calibrated level when a unit's line output is measured.

## Driver coverage

What `packages/chipvoice/src/driver.ts` writes when it plays a note. The core
implements more than this; the rest is written from the wiki and exercised by
nothing chipvoice plays.

| Voice | Exercised | Not exercised |
| --- | --- | --- |
| Pulse 1, 2 | `$4000` with duty, halt and a constant volume, rewritten when it changes; `$4001 = $08`, sweep off with negate set; `$4002`; `$4003` with length 31 at note start and whenever the period high bits change, which restarts the phase; silence is `$4000 = $30` | envelope decay; length counter expiry; the sweep unit in either direction |
| Triangle | `$4008 = $FF`, control flag with linear 127; `$400A`; `$400B` with length 31; silence is `$4008 = $00` then `$400B`, so the linear counter reloads with 0 | length or linear counter expiry by counting; the ultrasonic mute below period 2, which is implemented |
| Noise | `$400C` with halt and a constant volume; `$400E`; `$400F` with length 31 at note start; silence is `$400C = $30` | envelope decay; length expiry |
| DMC | | everything: no instrument reaches it. `load` puts a sample in memory, and the harness plays one |
| `$4015` | written once at power-on, `$0F` | clearing a bit mid-song |
| Frame counter | the 4-step sequence; `$4017` is never written | the 5-step sequence and the `$4017` write delay, which are implemented; the frame IRQ, which is not |

## Known deviations

| What | Deliberate | Why | Affects |
| --- | --- | --- | --- |
| The DMC's DMA does not stall a CPU, and its IRQ is a flag nothing reads | yes | there is no CPU | none for a driver; a test ROM that counts DMA cycles |
| The DMC's output unit powers on silent with eight bits remaining, as nesdev describes; a power-on state the documentation does not pin | no | no oracle agrees; Nes_Snd_Emu says one bit, Mesen says eight | the first sample byte's start, by up to seven bit periods |
| The frame IRQ is not implemented, and `$4015` cannot be read | yes | there is no CPU to interrupt or to read | `apu_test` cases that read the status register |
| `$4014` and `$4016` are ignored, and writes are decoded without the CPU's bus timing | yes | no CPU | none for a driver; a cycle-level oracle comparison may show a one-cycle offset on the write itself |
| Resampling is a box filter per output sample rather than band-limited step synthesis | partly | simple, fast, and the grit is wanted | aliasing on high pulse notes. Parity is measured before it; the analog sheet reports it |
| Output filters are first-order sections at the approximate corners, with no unit named, and a fixed gain of 2.9 | no | no measurement yet | the analog sheet |
| The envelope and the sweep are implemented from the wiki; the test ROMs do not reach them and the oracle clocks them two cycles late | no | nothing verifies them yet | an envelope or sweep step could be a cycle or two off; the length counters, which the ROMs do reach, are exact |

## Power-on state

`reset()` is power-on: every unit fresh, `$4015` and `$4017` at zero, nothing
queued. The cycle clock and the filters are left alone - the sample clock did not
stop, and zeroing a filter is a step through it. Power-on: every voice disabled
until `$4015` is written, the LFSR at 1 (as the hardware), the frame counter at
cycle 0 of the 4-step sequence, the triangle at step 0 (which outputs 15, as the hardware), every timer at 0, the DMC's
level at 0 with eight silent bits to go. Memory is not touched by a reset; a
cartridge is not. The output stage settles its filters on the first sample, so
the triangle's power-on 15 is not a step through them.

## History

Newest first.

- 2026-09-04 · The triangle powers on at step 0, output 15, as the hardware.
  Blargg's mixer test walks it from power-on to the sequence's zero and leaves
  it there; from the previous start it landed on 15 and detuned the whole
  mixing table, and the measurement said so. The output stage settles its
  filters on the first sample instead. Golden hash moved.
- 2026-09-04 · The frame IRQ flag, `$4015` read back, the reset button, and
  two things only the hardware knew: a halt flag written on a length clock's
  cycle takes effect after it, and a length reload on that cycle is ignored
  unless the counter was zero. Every one of blargg's APU test ROMs passes on a
  6502 the harness carries. Golden hash unchanged.
- 2026-09-04 · The DMC exists: a 7-bit level nudged by bits read from memory,
  at the rate table's pace, with `$4011` direct writes, looping, and `$4015`
  start and stop. Memory comes in through `load`, on the core, the worklet and
  the harness's logs. No instrument reaches it yet.
- 2026-09-04 · Register writes are bytes on `$4000-$4017`, decoded the way the
  chip decodes them. Two things the driver could do before and the hardware
  cannot are gone: a pulse's period high bits now change only through `$4003`,
  which restarts the phase, and `$4001` is written with `$08`, so pulse notes at
  period `$400` and above - G#2 and below - are no longer muted by the sweep
  unit. The 5-step frame sequence and the `$4017` write delay exist. Golden hash
  moved.
- 2026-09-04 · A write lands on the CPU cycle it is stamped with instead of at
  the start of the sample around it. Golden hash moved by the sub-sample shift.
- 2026-09-04 · The triangle holds its last value at note off instead of dropping
  to zero. Click on every bass note gone.
- 2026-09-04 · Half frames on the second and fourth steps of the sequence, not
  the first and third. Inaudible with the shipped driver.
- 2026-09-04 · The noise channel ran at half the hardware rate: the period table
  is in CPU cycles and the timer counted at the APU rate. Every drum was an
  octave dark. Golden hash changed with it.

## Sources

- [nesdev wiki, APU](https://www.nesdev.org/wiki/APU), and its pages on the pulse,
  triangle, noise, DMC, mixer, frame counter and length counter
- [Nes_Snd_Emu](https://github.com/blarggs-audio-libraries/Nes_Snd_Emu), blargg's
  reference, and the oracle planned for phase 1
- [blargg's APU test ROMs](https://github.com/christopherpow/nes-test-roms), for
  phase 1's bench
- Visual2A03, the netlist simulation, as the arbiter when references disagree

---

**Done** means: digital parity 100 % on the full corpus; every ROM above passes;
the analog stage within tolerance of the named unit; only deliberate deviations,
each with a reason; the golden hash recorded and the formula tests green in CI.
This sheet meets the last item and none of the others.
