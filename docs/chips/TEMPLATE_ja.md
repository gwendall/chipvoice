<a id="-"></a>
# <チップ名> (`<id>`)

<p align="center">
  <a href="TEMPLATE.md">English</a> &bull;
  <a href="TEMPLATE_ja.md">日本語</a>
</p>


チップ追加時にこのファイルを`docs/chips/<id>.md`へコピーし、その日に確認できた事実を書きます。全行が「未検証」でも有効です。各節の方法は[CONFORMANCE.md](../CONFORMANCE_ja.md)を参照してください。

| | |
| --- | --- |
| **機種** | |
| **状態** | 未検証 · 作業中 · 検証済み |
| **コア** | 独自 · `<project>`から移植 · `<project>`からコンパイル |
| **コアのライセンス** | |
| **更新日** | YYYY-MM-DD、手動 · `conform` `<version>`による生成 |

<a id="digital-parity"></a>
## デジタル一致率

リサンプリングとアナログ段の前の、サイクルごとのデジタル出力で測定します。

| | |
| --- | --- |
| 参照実装 | |
| コーパス | `<n>`ファイル、`<n>`秒 |
| 一致サイクル | `<n>` / `<n>` (`<xx.xxxx>` %) |
| 相違のあるファイル | |
| 最初の相違 | `<file>`、サイクル`<n>` |

<a id="test-roms"></a>
## テストROM

| ROM | 結果 | 備考 |
| --- | --- | --- |
| | 成功 · 失敗 · 未実行 | |

<a id="formula-tests"></a>
## 数式テスト

| テスト | 結果 |
| --- | --- |
| | |

<a id="analog-stage"></a>
## アナログ段

| | |
| --- | --- |
| 参照実機 | モデル、リビジョン、年、出力経路 |
| キャプチャ | レート、ビット深度、スクリプト |
| 許容差 | |
| 最大帯域誤差 | |
| 測定した境界条件 | |
| リサンプリング | |

<a id="driver-coverage"></a>
## ドライバーの対応範囲

配布ドライバーが実際に書き込む機能です。チップが実装していても、ここにない機能はchipvoiceの曲では使われず、コーパス／ROMだけで検証されるか、未検証のままです。

| ボイス | 使用する機能 | 使用しない機能 |
| --- | --- | --- |
| | | |

<a id="known-deviations"></a>
## 既知の差異

実機との差異をすべて記録します。意図的なものは理由を添えて維持し、それ以外は修正対象です。

| 内容 | 意図的か | 理由 | 影響 |
| --- | --- | --- | --- |
| | はい · いいえ | | |

<a id="power-on-state"></a>
## 電源投入状態

`reset()`で戻る状態と、実機の電源投入状態との差を記します。

<a id="history"></a>
## 履歴

音が変わった修正を、新しい順にコミットとともに記します。

<a id="sources"></a>
## 出典

実装の根拠にした資料と、検証に使った資料を記します。

---

**完了**とは、全コーパスでデジタル一致率100%、上記ROMがすべて成功、アナログ段が指定実機への許容差内、差異は理由付きの意図的なものだけ、ゴールデンハッシュを記録済み、CIの数式テストが成功している状態です。
