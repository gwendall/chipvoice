<a id="commodore-64-mos-6581-sid-c64"></a>
# Commodore 64：MOS 6581 SID (`c64`)

<p align="center">
  <a href="c64.md">English</a> &bull;
  <a href="c64_ja.md">日本語</a>
</p>


6581は3ボイスを備え、各ボイスに組み合わせ可能な4波形の発振器とゲート付きエンベロープがあります。隣接ボイス間の同期とリング変調、共有フィルターを使えます。方法は[CONFORMANCE.md](../CONFORMANCE_ja.md)にあります。

| | |
| --- | --- |
| **機種** | Commodore 64（PAL 985248 Hz、NTSCは1022727） |
| **状態** | **作業中**：各ボイスのデジタル2値がreSID-fpと一致、ドライバーは全役割を再生、アナログ段は資料由来のプロファイル |
| **コア** | 資料から独自実装（`packages/chipvoice/src/chips/c64/sid.ts`）。アナログプロファイルは`dsp.ts` |
| **コアのライセンス** | MIT。参照のreSID-fpはGPLで、ハーネス内だけに置く（決定18） |
| **更新日** | 2026-09-04、手動および`conform` |

<a id="digital-parity"></a>
## デジタル一致率

[`conform`](../../packages/conform)で[reSID-fp](../../packages/conform/oracles/residfp)と比較します。DAC前の各ボイスの12ビット波形と8ビットエンベロープが対象です。[コーパス](../../packages/conform/corpus/c64)はドライバー経由2曲と5スクリプト。マーカー内はハーネスが生成し（`pnpm --filter chipvoice-conform baseline:c64`）、解釈は手書きです。CIは一致数が基準未満なら失敗します。

<!-- parity:begin -->
`conform`による生成：2026-09-04。参照：reSID-fp (libsidplayfp, drfiemost), as a 6581。比較対象：osc1, osc2, osc3, env1, env2, env3。

| | |
| --- | --- |
| 参照 | reSID-fp (libsidplayfp, drfiemost), as a 6581 |
| コーパス | 7ログ、30542688サイクル |
| 一致サイクル | 30542688 / 30542688 (100.0000 %) |
| 相違のあるログ | 0 |

| ログ | 一致率 | 最初の相違 | ボイス別：一致率、エッジの完全一致／近似／不一致、最良の一定シフト、区間ごとのシフト整合 |
| --- | --- | --- | --- |
| script-combined | 100.0000 % | なし | osc1 100.0000 %, 51268/0/0; 区間 32: 32 時刻一致, 32 値一致, シフト <= 0; osc2 100.0000 %, 545/0/0; 区間 13: 13 時刻一致, 13 値一致, シフト <= 0; osc3 100.0000 %, 328347/0/0; 区間 8: 8 時刻一致, 8 値一致, シフト <= 0; env1 100.0000 %, 6291/0/0; 区間 25: 25 時刻一致, 25 値一致, シフト <= 0; env2 100.0000 %, 3231/0/0; 区間 13: 13 時刻一致, 13 値一致, シフト <= 0; env3 100.0000 %, 4251/0/0; 区間 17: 17 時刻一致, 17 値一致, シフト <= 0 |
| script-envelopes | 100.0000 % | なし | osc1 100.0000 %, 4186/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0; osc2 100.0000 %, 5275/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0; osc3 100.0000 %, 6272/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0; env1 100.0000 %, 8629/0/0; 区間 108: 108 時刻一致, 108 値一致, シフト <= 0; env2 100.0000 %, 6933/0/0; 区間 99: 99 時刻一致, 99 値一致, シフト <= 0; env3 100.0000 %, 10879/0/0; 区間 127: 127 時刻一致, 127 値一致, シフト <= 0 |
| script-noise | 100.0000 % | なし | osc1 100.0000 %, 118386/0/0; 区間 27: 27 時刻一致, 27 値一致, シフト <= 0; osc2 100.0000 %, 0/0/0; osc3 100.0000 %, 22940/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0; env1 100.0000 %, 936/0/0; 区間 4: 4 時刻一致, 4 値一致, シフト <= 0; env2 100.0000 %, 426/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; env3 100.0000 %, 426/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0 |
| script-sync-ring | 100.0000 % | なし | osc1 100.0000 %, 2434/0/0; 区間 3: 3 時刻一致, 3 値一致, シフト <= 0; osc2 100.0000 %, 619322/0/0; 区間 7: 7 時刻一致, 7 値一致, シフト <= 0; osc3 100.0000 %, 2218/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; env1 100.0000 %, 426/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; env2 100.0000 %, 4273/0/0; 区間 21: 21 時刻一致, 21 値一致, シフト <= 0; env3 100.0000 %, 426/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0 |
| script-waveforms | 100.0000 % | なし | osc1 100.0000 %, 949798/0/0; 区間 60: 60 時刻一致, 60 値一致, シフト <= 0; osc2 100.0000 %, 77900/0/0; 区間 31: 31 時刻一致, 31 値一致, シフト <= 0; osc3 100.0000 %, 204195/0/0; 区間 3: 3 時刻一致, 3 値一致, シフト <= 0; env1 100.0000 %, 7821/0/0; 区間 31: 31 時刻一致, 31 値一致, シフト <= 0; env2 100.0000 %, 4251/0/0; 区間 17: 17 時刻一致, 17 値一致, シフト <= 0; env3 100.0000 %, 681/0/0; 区間 3: 3 時刻一致, 3 値一致, シフト <= 0 |
| song-bright | 100.0000 % | なし | osc1 100.0000 %, 3292/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; osc2 100.0000 %, 869211/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; osc3 100.0000 %, 262063/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; env1 100.0000 %, 6349/0/0; 区間 93: 93 時刻一致, 93 値一致, シフト <= 0; env2 100.0000 %, 9766/0/0; 区間 27: 27 時刻一致, 27 値一致, シフト <= 0; env3 100.0000 %, 20402/0/0; 区間 142: 142 時刻一致, 142 値一致, シフト <= 0 |
| song-golden | 100.0000 % | なし | osc1 100.0000 %, 3292/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; osc2 100.0000 %, 868786/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; osc3 100.0000 %, 262071/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; env1 100.0000 %, 6349/0/0; 区間 93: 93 時刻一致, 93 値一致, シフト <= 0; env2 100.0000 %, 9656/0/0; 区間 27: 27 時刻一致, 27 値一致, シフト <= 0; env3 100.0000 %, 20419/0/0; 区間 150: 150 時刻一致, 150 値一致, シフト <= 0 |
<!-- parity:end -->

**数値の意味。** 全6ストリームの全ログが一致します。全波形の各音高とパルス幅、4組み合わせ波形とノイズ合成時のレジスタへの書き戻し、8レートのノイズとtestビットによるリセット、全ADSRレートとサステイン、各段で数サイクル間隔のゲート、アタック中に現在値より低く設定するレートレジスタ、同期済みの源を含む4比率での同期／リング変調、源のtestビットとゼロ周波数、ドライバー経由2曲を検査します。

初回に直した点は実機の事実に関する2点です。電源投入はリセット線の解除でノイズを1回進めます。またエンベロープのレート8はデータシートの391でなく392サイクルで、kevtrisが実チップから読んだ値に一致します。組み合わせ波形は各組み合わせ6数値のモデルで、全4096項目が参照表と一致します。`pnpm --filter chipvoice-conform fit:c64`で採点します。

<a id="test-roms"></a>
## テストROM

未実行。VICEの`testprogs/SID`は6510でOSC3／ENV3を読みますが、ハーネスにはまだそのCPUがありません（P7-7）。これらのプログラムと実機サンプリングに基づくreSID-fpを参照にします。

<!-- roms:begin -->
<!-- roms:end -->

<a id="formula-tests"></a>
## 数式テスト

毎pushで`packages/chipvoice/test/c64.mjs`を実行します。

| テスト | 結果 |
| --- | --- |
| 資料の周期から導く16レートがkevtrisの実測レジスタ値に一致 | 成功 |
| 440 Hzのパルスが毎秒880回反転し、三角波は0xffe、鋸歯状波は0xfffに達する | 成功 |
| ノイズは同じレジスタ値の有音高波形の16倍で進む | 成功 |
| 最速アタックは3 ms未満、最大値から最速リリースは6〜8 ms、sustain 8は0x88を保持 | 成功 |
| 同期ボイスは源の周期に従い、リング変調三角波は通常波と異なる | 成功 |
| 完全なラダーの重みは2の累乗。6581は同じ最大値に合計されるが各重みは異なる | 成功 |
| 和音とドラムをボイス3で共有し、同時には鳴らさず楽譜を再生 | 成功 |

<a id="analog-stage"></a>
## アナログ段

| | |
| --- | --- |
| 参照実機 | まだなし |
| キャプチャ | なし |
| 許容差 | |
| 最大帯域誤差 | 未測定 |
| 測定した境界条件 | なし |
| リサンプリング | PAL時計のサイクルを平均してホスト周期ごとに1サンプル、その後に出力段 |

`SID_6581_PROFILE`は資料由来です。各ボイスの2個のDACを終端なし・2R/R比2.2のR-2Rラダーとして扱い、非線形性を表します。波形DACのゼロ0x380はエンベロープのクリックの原因です。2積分器の状態変数フィルターはreSIDが6581 R4ARで測った曲線に従い、1024の折れ曲がりを含めます。Qは0.707〜1.707、出力段の境界は16 Hzと16 kHzです。自分たちの実機では未測定です。6581は他チップ以上に個体差があり、線形DACと別フィルターを持つ8580は未実装です。既知スクリプトの実機ライン出力が必要です（P7-8）。

<a id="driver-coverage"></a>
## ドライバーの対応範囲

`packages/chipvoice/src/chips/c64/driver.ts`の`SidDriver`を`test/c64-driver.mjs`で検査します。リードは1、ベースは2、和音とドラムは3です。ドラムが和音を中断し、その後に和音を戻す共有規則は`Sequencer.scheduleStep`にあります。

| ボイス | 使用する機能 | 使用しない機能 |
| --- | --- | --- |
| v1、v2、v3 | 4パルス幅、三角波、鋸歯状波。フレームごとの音高、2A03単位のベンド。最速アタック／ディケイで音量テーブルをサステインに適用、上昇は新ゲート、リリースは1段遅い。スネア用のフレームごとの波形変更 | 合成波形、同期、リング変調、testビット、最速以外のアタック／ディケイ、フィルター |
| ドラム用v3 | キックは1オクターブ下降する三角波、スネアはパルスクリックからノイズへ、ハイハットはノイズ。キット指定音高を使用 | |
| フィルター | 起動時に無効化 | 全機能：まだ意図から到達しない（P7-9） |

<a id="known-deviations"></a>
## 既知の差異

| 内容 | 意図的か | 理由 | 影響 |
| --- | --- | --- | --- |
| 指定サイクルを進める前に書き込み、同サイクル内は順序を保持 | はい | 6510はSID時計間で書き、参照側も同じ規約 | 最大1サイクルの到着時刻 |
| 合成波形は1台の6581 R2をkevtrisが測ったデータに基づくreSID-fp表への近似 | はい | 独自実測がない。隣接ビットが引き合う公開モデルを使用 | 別個体の合成波形ビット |
| testビットのノイズリセット時間と波形未選択時の減衰は温まった6581 R3相当 | はい | いずれもコンデンサー放電で個体と温度に依存。参照も同じ | test保持時間、OSC3が古い値を返す時間 |
| 8580は未モデル化 | 現時点でははい | 合成波形、三角波／鋸歯状波遅延、アナログ全体が異なる | C64後半世代 |

<a id="power-on-state"></a>
## 電源投入状態

`reset()`はリセット線です。レジスタ消去、ノイズ全1から1回進め、パルス比較器high、エンベロープはレートレジスタ0のリリースです。位相蓄積器は偶数ビットhigh、エンベロープカウンターは0xaaの電源投入値を保ち、時計開始後に最速で減衰します。参照作者の実測とreSID-fpに従います。ドライバーは音量最大、フィルター無効にします。

<a id="history"></a>
## 履歴

- 2026-09-04：チップ、ドライバー、コーパス。電源投入をリセットにしレート8を392へ修正後、全ログでreSID-fpと一致。

<a id="sources"></a>
## 出典

実装資料：

- MOS 6581データシート：レジスタ、波形、ms単位のエンベロープ、フィルター範囲。
- kevtris、SID envelope generator rate values（blog.kevtris.org）：15ビットレート比較値。資料の周期は1つを除き再現。
- plogue、SID 6581R3 ADSR tables up closeとNew research on SID ADSR：指数カウンター周期、上下両ニブルでのサステイン比較。
- VICE／reSIDのダイ解析とOSC3／ENV3サンプリングの公開知見：ノイズタップと2サイクルシフト、合成波形の書き戻し、ゲート状態のサイクル遷移、位相／カウンター起動値、DACラダー比のずれ、6581 R4ARのフィルター曲線。
