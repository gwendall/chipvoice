<a id="snes-s-dsp-snes"></a>
# SNES：S-DSP (`snes`)

<p align="center">
  <a href="snes.md">English</a> &bull;
  <a href="snes_ja.md">日本語</a>
</p>


スーパーファミコンの音源S-DSPは、ADSRとGAINエンベロープ付き8サンプルボイス、ガウス補間、ピッチ変調、ノイズ、8タップFIR付きエコーを備えます。SPC700と共有する64 KBからBRRサンプルを再生します。各節の方法は[CONFORMANCE.md](../CONFORMANCE_ja.md)にあります。

| | |
| --- | --- |
| **機種** | Super Nintendo、Super Famicom（SPC700クロック1024000 Hz、32クロックごとに1サンプル、32000 Hz） |
| **状態** | **作業中**：DSP出力はsnes_spcと一致、ドライバーは全役割を再生、アナログ段は未測定 |
| **コア** | snes_spcのSPC_DSPを逐行移植（`packages/chipvoice/src/chips/snes/sdsp.ts`） |
| **コアのライセンス** | `sdsp.ts`はsnes_spc由来のLGPL 2.1。独自コードはMIT。パッケージは両ライセンスを表示 |
| **更新日** | 2026-09-04、手動および`conform`による生成 |

<a id="digital-parity"></a>
## デジタル一致率

[`conform`](../../packages/conform)で、[`snes_spc`](../../packages/conform/oracles/snes-spc)とDSP出力ストリームを比較します。比較対象はDACへ渡す左右の16ビット値です。[コーパス](../../packages/conform/corpus/snes)のドライバー経由2曲と3スクリプトを使います。マーカー内はハーネスが生成し（`pnpm --filter chipvoice-conform baseline:snes`）、解釈は手書きです。CIは一致数がコミット済み基準を下回ると失敗します。

<!-- parity:begin -->
`conform`による生成：2026-09-04。参照：snes_spc 0.9.0 (blargg)。比較対象：left, right。

| | |
| --- | --- |
| 参照 | snes_spc 0.9.0 (blargg) |
| コーパス | 5ログ、23859200サイクル |
| 一致サイクル | 23859200 / 23859200 (100.0000 %) |
| 相違のあるログ | 0 |

| ログ | 一致率 | 最初の相違 | ボイス別：一致率、エッジの完全一致／近似／不一致、最良の一定シフト、区間ごとのシフト整合 |
| --- | --- | --- | --- |
| script-echo | 100.0000 % | なし | left 100.0000 %, 78790/0/0; 区間 60: 60 時刻一致, 60 値一致, シフト <= 0; right 100.0000 %, 78813/0/0; 区間 59: 59 時刻一致, 59 値一致, シフト <= 0 |
| script-envelopes | 100.0000 % | なし | left 100.0000 %, 142795/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0; right 100.0000 %, 142795/0/0; 区間 2: 2 時刻一致, 2 値一致, シフト <= 0 |
| script-pitch-noise-pmod | 100.0000 % | なし | left 100.0000 %, 54203/0/0; 区間 28: 28 時刻一致, 28 値一致, シフト <= 0; right 100.0000 %, 54203/0/0; 区間 28: 28 時刻一致, 28 値一致, シフト <= 0 |
| song-bright | 100.0000 % | なし | left 100.0000 %, 124507/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0; right 100.0000 %, 124506/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0 |
| song-golden | 100.0000 % | なし | left 100.0000 %, 124266/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0; right 100.0000 %, 124266/0/0; 区間 1: 1 時刻一致, 1 値一致, シフト <= 0 |
<!-- parity:end -->

**数値の意味。** このチップではデジタル出力がDACに渡す出力そのものです。ADSRと全GAINモード、複数レートの音高とノイズ、ピッチ変調、2種類のFIRを通す各遅延／帰還量のエコー、ドライバーバンクとバーストのBRR復号を比較し、移植初回から全ログで左右ともサンプル単位に一致しました。初回に見つかった問題はチップではなくプログラム側で、参照実装も同じビットを出しました。

実機キャプチャ由来の起動状態には注意点が2つあります。ESA位置から始まる28 KBのエコーバッファは、旧遅延が尽きて新EDLを読むまでRAM末尾を折り返してサンプル領域に書き込みます。また一部ボイスがキーオン済みで、停止したノイズ時計からエンベロープに従って増える定数が出ます。IPL ROMと同様に全ボイスを停止し、エコー書き込みを無効にして旧遅延を待つ処理を、ドライバー、スクリプト、数式テストへ入れました。

<a id="test-roms"></a>
## テストROM

未実行。2A03やDMGのような共同体のS-DSPテストROM集はありません。実機キャプチャに基づくsnes_spcが参照です。

<!-- roms:begin -->
<!-- roms:end -->

<a id="formula-tests"></a>
## 数式テスト

毎pushで`packages/chipvoice/test/snes.mjs`を実行します。

| テスト | 結果 |
| --- | --- |
| 440 Hzに設定したループ正弦波が毎秒440回ゼロ交差する | 成功 |
| 最大音量で16ビットの大部分に達し、キーオフで無音になる | 成功 |
| バーストが2 × 16 ms後にエコーで戻る | 成功 |
| BRR：64サンプルを正しいフラグの4ブロックに符号化し、入力に近い振幅で復号する | 成功 |

<a id="analog-stage"></a>
## アナログ段

| | |
| --- | --- |
| 参照実機 | まだなし |
| キャプチャ | なし |
| 許容差 | |
| 最大帯域誤差 | 未測定 |
| 測定した境界条件 | なし |
| リサンプリング | DACが32000 Hzの値を保持し、14 kHzの1次ローパスで段差を丸め、20 Hzのハイパスを適用。SPC700時計を進めてホスト周期ごとに1サンプル出力 |

DACと本体フィルターは仮モデルです。既知のスクリプトによる実機ライン出力が必要です（P6-8）。一部の本体で取得できるDSPデジタル出力ならトレースと直接比較できます。

<a id="driver-coverage"></a>
## ドライバーの対応範囲

`packages/chipvoice/src/chips/snes/driver.ts`の`SnesDriver`を`test/snes-driver.mjs`で検査します。リードは0、和音は1/4/5（拡張時1/4/5/6/7）、ベースは2、打楽器は3です。5音を超える和音は警告付きでアルペジオへ戻します。和音の振幅予算を音数で分け、控えめなステレオ配置を使います。

| ボイス | 使用する機能 | 使用しない機能 |
| --- | --- | --- |
| v0、v1、v2、v4–v7 | 独自BRRアタックと別サステインループ、音色群別ハードウェアADSR、フレームごとの音高／左右音量、キーオン、高速GAIN減衰によるノートオフ、エコー。従来の周期波形も利用可能 | 他のGAINモード、ピッチ変調、ハードウェアノイズ |
| v3 | バンクのワンショットドラムを`$1000`で再生、フレームごとの音量 | ハイハット用ノイズ源 |
| エコー | 有音高ボイスで有効：48 ms、帰還`$38`、多くのゲームで使われたローパスFIR。起動バッファの折り返し後に有効化 | 他のFIR、他の遅延 |

<a id="known-deviations"></a>
## 既知の差異

| 内容 | 意図的か | 理由 | 影響 |
| --- | --- | --- | --- |
| `$F3`書き込みは指定クロックの前に届き、同一クロックの複数書き込みは順序を維持 | はい | SPC700はDSPクロック間で書き込み、参照側も同じ規約 | 最大1クロックの到着時刻 |
| ノートオフはKOFFではなく各ボイスのGAIN | はい | KOFFは8ボイス共有。時間順でない予約では状態を保持できず、GAINはボイス固有 | 線形ではなく約8 msの指数減衰 |
| バンクは録音ではなく独自合成と符号化 | はい | チップではなくアレンジャーの楽器 | 意図の音色。チップの動作は変えない |

<a id="power-on-state"></a>
## 電源投入状態

`reset()`はsnes_spcの状態です。実SPCから取得したレジスタ、一部キーオン／ノイズ割り当て、28 KBエコー、ノイズレジスタ`$4000`、ゼロのカウンターを使います。ドライバーはエコー書き込みと出力を無効化し（読み取りも最初はサンプルRAMへ折り返し得る）、全ボイスをオフにし、ディレクトリ、音量、エコー、各エンベロープを設定してKOFFを解除します。起動バッファが折り返した0.25秒後にエコー書き込みと出力を有効にします。

工場出荷バンクは57,344からのエコーバッファより下の21,472バイトを使います。生成とBRR符号化はビルド時です。音量を`$1f`に制限し、飽和加算前に余裕を残します。任意の8ボイス効果音や直接レジスタ書き込みまで保証しません。[パレットの受け入れと測定](../SNES-PALETTE_ja.md)を参照してください。

<a id="history"></a>
## 履歴

- 2026-09-04：コア移植、ドライバー、コーパスを追加。初回から全ログ一致。起動時エコーとキーオン済みボイスはチップではなくプログラム側で処理。

<a id="sources"></a>
## 出典

実装資料：

- Shay Green、snes_spc 0.9.0：DSP。
- AnomieのSPC700／DSP資料、Fullsnes：レジスタとBRR。

検証先：

- `packages/conform/oracles/snes-spc`でネイティブビルドしたsnes_spc。
